// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

datasource db {
  provider = "mongodb" // Can be switched to MySQL or SQLite if preferred
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model DailyTransaction {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  date          DateTime @default(now())
  fodderType    String // 'Bajari' or 'Makai' – The type of fodder for the transaction
  sellQuantity  Float // Total quantity sold (kg) for the day
  sellAmount    Float? // Total amount earned from sales (₹) for the day
  buyQuantity   Float? // Total quantity bought (kg) for the day
  buyAmount     Float? // Total amount spent on purchases (₹) for the day
  stockQuantity Float? // Total quantity of fodder left at the end of the day (kg)
  stockValue    Float? // Total value of remaining stock (₹) at the end of the day
  avgBuyPrice   Float? // Average buy price per kilogram (₹) for the day
  avgSellPrice  Float? // Average sell price per kilogram (₹) for the day

  // Helper function to compute total stock value
  // @computed
  // getTotalStockValue() {
  //   return stockQuantity * avgBuyPrice
  // }
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
